apiVersion: apps/v1
kind: Deployment
metadata:
  name: "Partycipate"
spec:
  selector:
    matchLabels:
      app: "Partycipate"
  template:
    metadata:
      labels:
        app: "Partycipate"
    spec:
      containers:
        - name: "Partycipate"
          image: "gcr.io/Partycipate/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "Partycipate"
spec:
  selector:
    app: "Partycipate"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080